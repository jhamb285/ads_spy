# ========================================
# PROJECT 1: ADSPY TOOL - ENVIRONMENT VARIABLES
# ========================================

# ====================
# REQUIRED VARIABLES
# ====================

# Apify API Token (for scraping Facebook ads)
APIFY_API_TOKEN=your_apify_token_here

# OpenAI API Key (for GPT-4 analysis, Vision, Whisper)
OPENAI_API_KEY=sk-your_openai_key_here

# Gemini API Key (for initial ad breakdown)
# NEW: Added for dual-AI analysis
GEMINI_API_KEY=your_gemini_key_here

# Offer Description (used for rewriting ads for your offer)
OFFER_DESCRIPTION="Your product/service description here"

# Database URL (PostgreSQL connection string)
# NEW: Required for database-backed storage
# Format: postgresql://username:password@host:5432/database_name
DATABASE_URL=postgresql://creative_os_user:your_password@localhost:5432/creative_os

# ====================
# OPTIONAL VARIABLES
# ====================

# Slack Configuration
# Bot Token (starts with xoxb-)
SLACK_TOKEN=xoxb-your-slack-bot-token

# App Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-slack-app-token

# Slack Channel (default: adspy-feed)
SLACK_CHANNEL=adspy-feed

# Scraping Configuration
# Maximum ads to fetch per brand (default: 10)
MAX_ADS_PER_BRAND=10

# Only fetch ads from last N days (optional)
DAYS_BACK=30

# Minimum days ad must be active to be analyzed (optional, global default)
MIN_ACTIVE_DAYS=5

# API Configuration
# API server port (default: 1002 for Project 1)
API_PORT=1002

# Frontend URL for CORS (default: http://localhost:1001)
FRONTEND_URL=http://localhost:1001

# ====================
# GOOGLE SHEETS (Optional - for hook generator & brand management)
# ====================
# NEW: Google Sheets can now be used for easy brand management
# Create a "Brands" tab in your sheet with columns:
#   - Avatar, Brand Name, Facebook Page URL, Active, Min Active Days
# The system will auto-sync brands on startup (bot/batch mode)
# Run 'npm run sync-brands' to manually sync anytime

GOOGLE_SHEETS_ID=your_google_sheet_id
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_KEY="-----BEGIN PRIVATE KEY-----\nYour_Private_Key_Here\n-----END PRIVATE KEY-----\n"

# ====================
# NOTES
# ====================

# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to version control
# 3. DATABASE_URL is required for the new database-backed system
# 4. GEMINI_API_KEY is optional but recommended for better ad analysis
# 5. Slack tokens are optional for CLI mode, required for bot mode
# 6. Run 'npm run setup-db' to create database tables
# 7. Run 'npm run migrate-brands' to import default brands to database
# 8. GOOGLE_SHEETS integration allows easy brand management via spreadsheet
#    - See GOOGLE_SHEETS_SETUP.md for detailed setup instructions
#    - Brands auto-sync on bot/batch startup if GOOGLE_SHEETS_ID is set
#    - Run 'npm run sync-brands' for manual sync anytime
